# Placeholder
## Q1(pdf Q#37)
최근 한 회사가 AWS 계정의 Amazon EC2 인스턴스에 다양한 새로운 워크로드를 배포했습니다. 이 회사는 인스턴스에 원격으로 안전하게 액세스하고 관리하기 위한 전략을 수립해야 합니다.
이 회사는 AWS 네이티브 서비스와 연동되고 AWS Well-Architected Framework를 준수하는 반복 가능한 프로세스를 구현해야 합니다.
다음 중 운영 오버헤드가 가장 적은 솔루션은 무엇입니까?

A. EC2 시리얼 콘솔을 사용하여 각 인스턴스의 터미널 인터페이스에 직접 액세스하여 관리합니다.
B. 기존 인스턴스와 새 인스턴스 각각에 적절한 IAM 역할을 연결합니다. AWS Systems Manager Session Manager를 사용하여 원격 SSH 세션을 설정합니다.
C. 관리용 SSH 키 쌍을 생성합니다. 각 EC2 인스턴스에 공개 키를 로드합니다. 공용 서브넷에 배스천 호스트를 배포하여 각 인스턴스 관리를 위한 터널을 제공합니다.
D. AWS 사이트 간 VPN 연결을 설정합니다. 관리자에게 온프레미스 로컬 머신을 사용하여 VPN 터널을 통해 SSH 키를 이용해 인스턴스에 직접 연결하도록 지시합니다.

AWS Systems Manager Session Manager:
ssh키 불필요
퍼블릭 ip, vpn 불필요
IAM기반 접근 제어
보안 + 운영 효율 + AWS 모범 사례 = session manager

EC2 인스턴스에 IAM Role 연결
해당 Role에 AmazonSSMManagedInstanceCore 권한 부여
Session Manager로 콘솔/CLI에서 바로 접속

정답 : B

## Q2(pdf Q#84)
한 회사가 기존 3계층 웹 아키텍처의 비용을 절감하고자 합니다. 웹, 애플리케이션 및 데이터베이스 서버는 Amazon EC2 인스턴스에서 실행되고 있습니다.
개발, 테스트 및 프로덕션 환경에 대해 EC2 인스턴스가 사용됩니다. EC2 인스턴스는 피크 시간대에 평균 30%의 CPU 사용률을 보이고,
비피크 시간대에는 10%의 CPU 사용률을 보입니다.
프로덕션 EC2 인스턴스는 24시간 내내 실행됩니다. 개발 및 테스트 EC2 인스턴스는 매일 최소 8시간 이상 실행됩니다. 이 회사는
사용하지 않을 때 개발 및 테스트 EC2 인스턴스를 자동으로 중지하는 자동화 시스템을 구현할 계획입니다.

어떤 EC2 인스턴스 구매 솔루션이 이 회사의 요구 사항을 가장 비용 효율적으로 충족할까요?

A. 프로덕션 EC2 인스턴스에는 스팟 인스턴스를 사용하고, 개발 및 테스트 EC2 인스턴스에는 예약 인스턴스를 사용합니다.
B. 프로덕션 EC2 인스턴스에는 예약 인스턴스를 사용하고, 개발 및 테스트 EC2 인스턴스에는 온디맨드 인스턴스를 사용합니다.
C. 프로덕션 EC2 인스턴스에는 스팟 블록을 사용하고, 개발 및 테스트 EC2 인스턴스에는 예약 인스턴스를 사용합니다.
D. 프로덕션 EC2 인스턴스에는 온디맨드 인스턴스를 사용하고, 개발 및 테스트 EC2 인스턴스에는 스팟 블록을 사용하십시오.

프로덕션 → 예약 인스턴스
24시간 × 365일 → 지속 사용
중단 불가, 안정성 최우선

개발/테스트 → 온디맨드 인스턴스
하루 일부 시간만 사용
자동 중지 예정 -> 사용 시간 불규칙

정답 : B

## Q3(pdf Q#127)
한 미디어 회사가 시스템을 AWS 클라우드로 이전하는 방안을 검토 중입니다. 이 회사는 비디오 처리를 위해 최대 I/O 성능을 갖춘 최소 10TB의 스토리지, 미디어 콘텐츠 저장을 위한 300TB의 매우 내구성이 뛰어난 스토리지, 그리고 더 이상 사용하지 않는 아카이브 미디어 저장을 위한 900TB의 스토리지가 필요합니다.
이러한 요구 사항을 충족하기 위해 솔루션 아키텍트는 어떤 서비스 조합을 추천해야 할까요?

A. 최대 성능을 위한 Amazon EBS, 내구성 있는 데이터 스토리지를 위한 Amazon S3, 아카이브 스토리지를 위한 Amazon S3 Glacier
B. 최대 성능을 위한 Amazon EBS, 내구성 있는 데이터 스토리지를 위한 Amazon EFS, 아카이브 스토리지를 위한 Amazon S3 Glacier
C. 최대 성능을 위한 Amazon EC2 인스턴스 스토어, 내구성 있는 데이터 스토리지를 위한 Amazon EFS, 아카이브 스토리지를 위한 Amazon S3
D. 최대 성능을 위한 Amazon EC2 인스턴스 스토어, 내구성 있는 데이터 스토리지를 위한 Amazon S3, 아카이브 스토리지를 위한 Amazon S3 Glacier

정답 : D

## Q4(pdf Q#543)
한 회사가 여러 AWS 계정에서 Amazon EC2 인스턴스를 운영하고 있으며, 각 계정은 개별적으로 청구됩니다. 이 회사는 최근 Savings Plan을 구매했습니다.
회사의 비즈니스 요구 사항 변경으로 인해 많은 EC2 인스턴스를 폐기했습니다. 이 회사는
Savings Plan 할인을 다른 AWS 계정에서도 사용하려고 합니다.
다음 중 어떤 단계 조합이 이러한 요구 사항을 충족합니까? (두 가지를 선택하십시오.)

A. 관리 계정의 AWS 계정 관리 콘솔에서 청구 기본 설정 섹션의 할인 공유를 활성화합니다.
B. 기존 Savings Plan을 구매한 계정의 AWS 계정 관리 콘솔에서 청구 기본 설정 섹션의 할인 공유를 활성화합니다.
모든 계정을 포함합니다.
C. AWS Organizations 관리 계정에서 AWS Resource Access Manager(AWS RAM)를 사용하여 Savings Plan을 다른 계정과 공유합니다.
D. 새 결제 계정의 AWS Organizations에 조직을 생성합니다. 관리 계정에서 다른 AWS 계정을 해당 조직에 초대합니다.
E. 기존 EC2 인스턴스와 Savings Plan이 있는 기존 AWS 계정에서 AWS Organizations에 조직을 생성합니다. 관리 계정에서 다른 AWS 계정을 해당 조직에 초대합니다.

현재 각 계정이 개별 청구 → 할인 공유 불가 상태
Savings Plan을 이미 구매한 계정이 관리 계정이 되어
다른 AWS 계정들을 조직에 초대
-> 통합 결제 환경이 만들어짐
이단계가 없으면 계정 간 할인 공유 자체가 불가능하다.

AWS Organizations 관리 계정에서만 가능
AWS 계정 관리 콘솔
→ 청구(Billing)
→ 결제 기본 설정(Billing preferences)
→ 할인 공유 활성화
이 설정을 켜야 Savings Plan 혜택이 조직 내 다른 계정 EC2 사용량에 자동 적용

정답 : A , E

## Q5(pdf Q#24)
한 회사가 최근 청구서에서 Amazon EC2 비용이 증가한 것을 확인했습니다. 청구팀은 몇몇 EC2 인스턴스에서 원치 않는 인스턴스 유형의 수직 확장이 발생한 것을 발견했습니다.
솔루션 아키텍트는 지난 2개월간의 EC2 비용을 비교하는 그래프를 생성하고 심층 분석을 수행하여 수직 확장의 근본 원인을 파악해야 합니다.
솔루션 아키텍트는 운영 오버헤드를 최소화하면서 어떻게 이 정보를 생성해야 할까요?

빠르게 원인 파악 + 운영 오버헤드 최소

A. AWS Budgets를 사용하여 예산 보고서를 생성하고 인스턴스 유형별 EC2 비용을 비교합니다.
B. Cost Explorer의 세부 필터링 기능을 사용하여 인스턴스 유형별 EC2 비용에 대한 심층 분석을 수행합니다.
C. AWS 청구 및 비용 관리 대시보드의 그래프를 사용하여 지난 2개월간의 인스턴스 유형별 EC2 비용을 비교합니다.
D. AWS 비용 및 사용량 보고서를 사용하여 보고서를 생성하고 Amazon S3 버킷에 저장합니다. Amazon QuickSight와 Amazon S3를 소스로 사용하여 인스턴스 유형별 대화형 그래프를 생성합니다.

Amazon Cost Explorer
지난 2개월 EC2 비용을 그래프로 바로 비교 가능
인스턴스 유형 기준으로 필터링 / 그룹화 가능
추가 인프라, 데이터 파이프라인, 시각화 도구 필요 없음
최소롸의 클릭으로 심층 분석 가능 -> 운영 오버헤드 최소

EC2 비용 증가 원인을 빠르고 가볍게 분석하려면
-> 인스턴스 유형별 비용을 드릴다운 분석

정답 : B

## Q6(pdf Q#47)
한 회사가 1주일 동안 진행될 예정인 행사를 위해 특정 AWS 리전의 특정 가용 영역 3개에 대해 보장된 Amazon EC2 용량이 필요합니다.
이 회사는 EC2 용량을 보장받기 위해 어떻게 해야 할까요?

짧은 기간 + 특정 리전 + 특정 AZ 3개에서 용량 보장

A. 필요한 리전을 지정하는 예약 인스턴스를 구매합니다.
B. 필요한 리전을 지정하는 온디맨드 용량 예약을 생성합니다.
C. 필요한 리전과 3개의 가용 영역을 지정하는 예약 인스턴스를 구매합니다.
D. 필요한 리전과 3개의 가용 영역을 지정하는 온디맨드 용량 예약을 생성합니다.

온디맨드 용량 예약 : 
특정 리전 + 특정 AZ 지정 가능
즉시 용량 보장
단기 사용의 최적화 되어 있음
비용 할인 목적 아님 (용량 보장이 목적)

정답 : D

## Q7(pdf Q#50)
한 회사가 1,000개의 Amazon EC2 Linux 인스턴스에서 실행되는 프로덕션 워크로드를 보유하고 있습니다. 이 워크로드는 타사 소프트웨어로 구동됩니다.
이 회사는 심각한 보안 취약점을 해결하기 위해 모든 EC2 인스턴스의 타사 소프트웨어에 최대한 빨리 패치를 적용해야 합니다.

솔루션 아키텍트는 이러한 요구 사항을 충족하기 위해 무엇을 해야 할까요?

A. 모든 EC2 인스턴스에 패치를 적용하는 AWS Lambda 함수를 생성합니다.
B. 모든 EC2 인스턴스에 패치를 적용하도록 AWS Systems Manager Patch Manager를 구성합니다.
C. 모든 EC2 인스턴스에 패치를 적용하기 위해 AWS Systems Manager 유지 관리 기간을 예약합니다.
D. AWS Systems Manager Run Command를 사용하여 모든 EC2 인스턴스에 패치를 적용하는 사용자 지정 명령을 실행합니다.

정답 : B

A : AWS Lambda는 서버 관리용 도구가 아님
C : Systems Manager 유지 관리 기간은 패치를 언제 적용할지 쓰는 기능이 아님(스케줄 용도)
D : 가능은 하지만 전용서비스가 아니다

## Q8(pdf Q#54)
한 회사가 AWS에서 여러 Windows 워크로드를 실행하고 있습니다. 회사 직원들은 두 개의 Amazon EC2 인스턴스에 호스팅된 Windows 파일 공유를 사용합니다.
파일 공유는 서로 데이터를 동기화하고 복사본을 유지합니다. 회사는 사용자가 현재 파일에 액세스하는 방식을 유지하면서 고가용성과 내구성이 뛰어난 스토리지 솔루션을 원합니다.
이러한 요구 사항을 충족하기 위해 솔루션 아키텍트는 무엇을 해야 할까요?

A. 모든 데이터를 Amazon S3로 마이그레이션합니다. 사용자가 파일에 액세스할 수 있도록 IAM 인증을 설정합니다.
B. Amazon S3 파일 게이트웨이를 설정합니다. 기존 EC2 인스턴스에 S3 파일 게이트웨이를 마운트합니다.
C. 파일 공유 환경을 다중 가용 영역(Multi-AZ) 구성의 Amazon FSx for Windows File Server로 확장합니다. 모든 데이터를 FSx for Windows File Server로 마이그레이션합니다.
D. 파일 공유 환경을 다중 가용 영역(Multi-AZ) 구성의 Amazon Elastic File System(Amazon EFS)으로 확장합니다. 모든 데이터를 Amazon EFS로 마이그레이션합니다.

Amazon FSx for Windows File Server
높은 내구성과 가용성
사용ㅈ능 기존에 사용하던 방식과 동일하게 네트워크 드라이브로 접근하면 된다.
EC2 간 파일 동기화 불필요 -> 운영 부담 제거

-> EC2 두대 + 파일 동기화 구조를 관리형 서비스로 깔끔하게 대처 하는 방법이 된다.

정답 : C

## Q9(pdf Q#68)
솔루션 아키텍트가 회사의 온프레미스 인프라를 AWS로 확장하기 위한 새로운 하이브리드 아키텍처를 설계하고 있습니다. 이 회사는 AWS 리전에 대한 고가용성 및 일관된 저지연 연결이 필요합니다. 또한 비용을 최소화해야 하며, 기본 연결에 실패할 경우 트래픽 속도가 느려지는 것을 감수할 의향이 있습니다.

솔루션 아키텍트는 이러한 요구 사항을 충족하기 위해 무엇을 해야 할까요?

A. 리전에 대한 AWS Direct Connect 연결을 프로비저닝합니다. 기본 Direct Connect 연결에 실패할 경우를 대비하여 백업 VPN 연결을 프로비저닝합니다.
B. 프라이빗 연결을 위해 리전에 대한 VPN 터널 연결을 프로비저닝합니다. 프라이빗 연결을 위한 두 번째 VPN 터널을 프로비저닝하고, 기본 VPN 연결에 실패할 경우를 대비한 백업 연결을 프로비저닝합니다.
C. 리전에 대한 AWS Direct Connect 연결을 프로비저닝합니다. 기본 Direct Connect 연결에 실패할 경우를 대비하여 동일한 리전에 대한 두 번째 Direct Connect 연결을 백업으로 프로비저닝합니다.
D. 리전에 대한 AWS Direct Connect 연결을 프로비저닝합니다. AWS CLI의 Direct Connect 페일오버 속성을 사용하여 기본 Direct Connect 연결에 실패할 경우 백업 연결을 자동으로 생성합니다.

aws direct connect : 일관된 저지연, 안정적인 프라이빗 연결
백업 vpn : 비용 저렴(최소화) 장애시 자동으로 페일오버 가능
페일오버 : 서버, 시스템, 네트워크 등에서 주 장비에 장애가 발생했을 때, 예비 시스템으로 자동으로 전환되어 서비스 중단을 최소화하고 연속성을 보장하는 기술

정답 : A

## Q10(pdf Q#124)
한 회사에서 매우 동적인 배치 처리 작업을 수행하고 있는데, 이 작업은 완료하는 데 여러 Amazon EC2 인스턴스를 사용합니다. 이 작업은 상태를 저장하지 않는 특성을 가지며,
언제든지 시작하거나 중지해도 부정적인 영향을 미치지 않습니다. 일반적으로 완료하는 데 총 60분 이상 소요됩니다. 이 회사는
솔루션 아키텍트에게 이 작업의 요구 사항을 충족하는 확장 가능하고 비용 효율적인 솔루션을 설계해 달라고 요청했습니다.
솔루션 아키텍트는 어떤 솔루션을 권장해야 할까요?

A. EC2 스팟 인스턴스를 구현합니다.
B. EC2 예약 인스턴스를 구매합니다.
C. EC2 온디맨드 인스턴스를 구현합니다.
D. AWS Lambda에서 처리를 구현합니다.

EC2 스팟 인스턴스
온디맨드에 비해 70~90%정도의 가격으로 EC2 인스턴스를 이용할 수 있게 해주는 기능
일회성이나 영구적으로 사용하는 방법이 있는데 영구적 사용시 인스턴스가 종료되고 다시 인스턴스 풀에 사용할 수 있는 인스턴스가 발견되면 사용하게 해준다. -> 종료 되어도 괜찮다.

중단 돼도 괜찮다 -> 스팟 인스턴스

정답 : A

## Q11(pdf Q#128)
한 회사가 AWS 클라우드에서 컨테이너 기반 애플리케이션을 실행하려고 합니다. 이러한 애플리케이션은 상태를 저장하지 않으며, 기본 인프라의 장애를 허용할 수 있습니다.
이 회사는 비용과 운영 오버헤드를 최소화하는 솔루션을 필요로 합니다.
이러한 요구 사항을 충족하기 위해 솔루션 아키텍트는 무엇을 해야 할까요?

A. Amazon EC2 Auto Scaling 그룹의 스팟 인스턴스를 사용하여 애플리케이션 컨테이너를 실행합니다.
B. Amazon Elastic Kubernetes Service(Amazon EKS) 관리형 노드 그룹의 스팟 인스턴스를 사용합니다.
C. Amazon EC2 Auto Scaling 그룹의 온디맨드 인스턴스를 사용하여 애플리케이션 컨테이너를 실행합니다.
D. Amazon Elastic Kubernetes Service(Amazon EKS) 관리형 노드 그룹의 온디맨드 인스턴스를 사용합니다.

스팟 인스턴스 -> 비용 최소화
애플리케이션이 무상태라 스팟 중단 문제 없음
무상태 + 장애 허용 특성 활용
EKS : aws에서 Kubernetes를 관리형으로 쓰게 해주는 서비스 
사람이 직접 손으로 관리하면 최악이다

정답 : B

## Q12(pdf Q#140)
솔루션 아키텍트는 한 회사가 AWS에서 애플리케이션을 실행하는 비용을 최적화하도록 지원해야 합니다. 이 애플리케이션은 아키텍처 내에서 컴퓨팅을 위해 Amazon EC2 인스턴스, AWS Fargate 및 AWS Lambda를 사용합니다.
EC2 인스턴스는 애플리케이션의 데이터 수집 계층을 실행합니다. EC2 사용량은 불규칙적이고 예측하기 어렵습니다. EC2 인스턴스에서 실행되는 워크로드는 언제든지 중단될 수 있습니다.
애플리케이션 프런트엔드는 Fargate에서 실행되고 Lambda는 API 계층을 제공합니다. 프런트엔드 및 API 계층의 사용률은 향후 1년 동안 예측 가능합니다.
다음 중 어떤 구매 옵션 조합이 이 애플리케이션을 호스팅하는 데 가장 비용 효율적인 솔루션을 제공할까요? (두 가지를 선택하세요.)

A. 데이터 수집 계층에 스팟 인스턴스 사용
B. 데이터 수집 계층에 온디맨드 인스턴스 사용
C. 프런트엔드 및 API 계층에 1년 컴퓨팅 절약 플랜 구매
D. 데이터 수집 계층에 1년 선불 예약 인스턴스 구매
E. 프런트엔드 및 API 계층을 위해 1년 EC2 인스턴스 절약 플랜을 구매하세요.

정답 : A,C
compute savings plan :

스팟인스턴스 -> 비용최소화(11번에서 정리)

EC2
Fargate
Lambda
전부 적용 가능

## Q13(pdf Q#167)
한 회사가 Amazon EC2 인스턴스 세트에서 프로덕션 애플리케이션을 운영하고 있습니다. 이 애플리케이션은 Amazon SQS 큐에서 데이터를 읽어와 메시지를 병렬로 처리합니다. 메시지 볼륨은 예측할 수 없고 간헐적인 트래픽이 자주 발생합니다. 이 애플리케이션은 다운타임 없이 지속적으로 메시지를 처리해야 합니다.

다음과 같은 솔루션이 이러한 요구 사항을 가장 비용 효율적으로 충족합니까?

A. 필요한 최대 용량을 처리하기 위해 스팟 인스턴스만 사용합니다.
B. 필요한 최대 용량을 처리하기 위해 예약 인스턴스만 사용합니다.
C. 기본 용량에는 예약 인스턴스를 사용하고 추가 용량에는 스팟 인스턴스를 사용합니다.
D. 기본 용량에는 예약 인스턴스를 사용하고 추가 용량에는 온디맨드 인스턴스를 사용합니다.

정답 : C

## Q14(pdf Q#219)
한 회사의 애플리케이션에 성능 문제가 발생하고 있습니다. 이 애플리케이션은 상태를 유지하며 Amazon EC2 인스턴스에서 메모리 내 작업을 완료해야 합니다.
이 회사는 AWS CloudFormation을 사용하여 인프라를 배포하고 M5 EC2 인스턴스 제품군을 사용했습니다. 트래픽이 증가함에 따라 애플리케이션 성능이 저하되었습니다.
사용자들이 애플리케이션에 접속하려고 할 때 지연이 발생한다고 보고하고 있습니다.
이러한 문제를 가장 운영 효율적인 방식으로 해결할 수 있는 방법은 무엇입니까?

A. EC2 인스턴스를 Auto Scaling 그룹에서 실행되는 T3 EC2 인스턴스로 교체합니다. AWS Management Console을 사용하여 변경합니다.
B. CloudFormation 템플릿을 수정하여 EC2 인스턴스를 Auto Scaling 그룹에서 실행하도록 합니다. 필요한 경우 Auto Scaling 그룹의 원하는 용량과 최대 용량을 수동으로 늘립니다.
C. CloudFormation 템플릿을 수정합니다. EC2 인스턴스를 R5 EC2 인스턴스로 교체합니다. Amazon CloudWatch의 내장 EC2 메모리 메트릭을 사용하여 애플리케이션 성능을 추적하고 향후 용량 계획을 수립합니다.
D. CloudFormation 템플릿을 수정합니다. EC2 인스턴스를 R5 EC2 인스턴스로 교체합니다. 향후 용량 계획을 위해 사용자 지정 애플리케이션 지연 시간 지표를 생성하려면 EC2 인스턴스에 Amazon CloudWatch 에이전트를 배포합니다.

기본 처리량 : 예약인스턴스 -> 항상 안정적으로 처리한다
트래픽 급증시 : 스팟 인스턴스 -> 저렴하게 확장
sqs : 메시지 재처리 가능, 추가 용량

정답 : C

## Q15(pdf Q#231)
애플리케이션이 VPC A에 있는 탄력적 IP 주소를 가진 Amazon EC2 인스턴스에서 실행됩니다. 이 애플리케이션은 VPC B에 있는 데이터베이스에 액세스해야 합니다.
두 VPC는 ​​동일한 AWS 계정에 있습니다.
다음 중 필요한 액세스를 가장 안전하게 제공하는 솔루션은 무엇입니까?

A. VPC A에 있는 애플리케이션 서버의 공용 IP 주소에서 모든 트래픽을 허용하는 DB 인스턴스 보안 그룹을 생성합니다.
B. VPC A와 VPC B 사이에 VPC 피어링 연결을 구성합니다.
C. DB 인스턴스를 공개적으로 액세스할 수 있도록 합니다. DB 인스턴스에 공용 IP 주소를 할당합니다.
D. VPC B에 탄력적 IP 주소를 가진 EC2 인스턴스를 시작합니다. 모든 요청을 새 EC2 인스턴스를 통해 프록시합니다.

VPC 피어링 연결 구성

VPC A <-> VPC B 프라이빗 통신
트래픽 : AWS 백본 네트워크만 사용
보안그룹에서 : VPC CIDR 기반 허용 가능
추가 인프라 확장 필요 없음

-> 가장 안전한 방법이다

정답 : B




## 최종 정리

앞부분에서 한번 정리해둔 내용이나 문제들이 계속해서 나오는경우를 볼 수 있었다. 반복해서 나오는 문제나 답지의 경우에는 한번 더 정리해서 공부하는게 좋을거 같았다.